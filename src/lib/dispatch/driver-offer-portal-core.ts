import type { OperationalBooking } from "./booking-types";
import { generalArea } from "./offer-core.ts";
import type { DispatchOfferRecord } from "./offer-types";
import type { DriverOfferResponse,SecureDriverOfferProjection } from "./driver-offer-portal-types";
const responsibility=(label:string,value:unknown)=>`${label}: ${value==="customer"?"Customer responsibility":value==="rentka"?"RentKA/vendor responsibility":value==="included"?"Included":"As agreed with RentKA"}`;
export function createSecureDriverOfferProjection(booking:OperationalBooking,offer:DispatchOfferRecord,responses:DriverOfferResponse[]=[]):SecureDriverOfferProjection{const r=booking.responsibilities??{},latest=responses[0],duration=typeof booking.sourceSnapshot.durationHours==="number"?`${booking.sourceSnapshot.durationHours} hours`:"Confirm package with RentKA";return{bookingOperationalId:booking.id,bookingId:booking.bookingId,offerId:offer.id,driverId:offer.driverId,vendorId:offer.vendorId,offeredPayoutMinor:offer.currentOfferedPayoutMinor??offer.approvedPayoutMinor,generalPickupArea:generalArea(booking.itinerary.pickup),generalDestination:generalArea(booking.itinerary.destinationOrUsage),travelDate:booking.itinerary.travelDate,pickupTime:booking.itinerary.pickupTime,vehicleRequirement:booking.requestedVehicle.categoryOrModel,packageInformation:duration,responsibilities:[responsibility("Fuel",r.fuel),responsibility("Toll",r.toll),responsibility("Parking",r.parking)],supportPhone:"0302 058 9999",...(offer.secureViewedAt?{viewedAt:offer.secureViewedAt}:{}),closed:offer.portalStatus==="closed"||booking.lifecycle!=="active"||booking.assignment?.status==="assigned",...(latest?{latestResponse:latest}:{})}}
export function validateCounterPayout(amountMinor:number,offeredMinor:number){if(!Number.isSafeInteger(amountMinor)||amountMinor<=0)throw new Error("Counter payout must be a valid positive amount.");if(amountMinor>Math.max(offeredMinor*5,5000000))throw new Error("Counter payout is outside the reasonable review range.");return amountMinor}
