import type { MatchCandidate, MatchProjection } from "@/lib/dispatch/matching-types";
import { setMatchOverrideAction } from "./actions";
import type { DispatchOfferRecord, DriverOfferProjection } from "@/lib/dispatch/offer-types";
import OfferControls from "./OfferControls";

const CandidateCard=({bookingDocumentId,candidate,rank,offerProjection,offer}:{bookingDocumentId:string;candidate:MatchCandidate;rank?:number;offerProjection?:DriverOfferProjection;offer?:DispatchOfferRecord})=><article className={`rounded-xl border ${candidate.manuallyIncluded?"border-purple-300 bg-purple-50":"border-green-200 bg-green-50"} p-4`}><div className="flex flex-wrap items-start justify-between gap-2"><div><p className="font-black text-[#0F2B46]">{rank?`${rank}. `:""}{candidate.vendor.name}</p><p className="font-bold">{candidate.driver.name} + {candidate.vehicle.label} — {candidate.vehicle.registrationNumber}</p><p className="text-sm">Score: {candidate.score} · {candidate.compatibility.replaceAll("_"," ")}</p></div><form action={setMatchOverrideAction}><input type="hidden" name="bookingDocumentId" value={bookingDocumentId}/><input type="hidden" name="candidateId" value={candidate.id}/><input type="hidden" name="mode" value="exclude"/><button className="rounded-lg border border-red-300 bg-white px-3 py-1.5 text-xs font-bold text-red-700">Exclude</button></form></div><ul className="mt-3 grid gap-1 text-sm sm:grid-cols-2">{candidate.reasons.map(reason=><li key={reason}>• {reason}</li>)}</ul>{offerProjection?<OfferControls projection={offerProjection} offer={offer}/>:<p className="mt-3 rounded bg-red-50 p-2 text-xs font-bold text-red-700">Driver WhatsApp number is invalid or missing. Offer actions are unavailable.</p>}</article>;
export default function MatchPanel({bookingDocumentId,projection,offerProjections=[],offers=[]}:{bookingDocumentId:string;projection:MatchProjection;offerProjections?:DriverOfferProjection[];offers?:DispatchOfferRecord[]}){const offerFor=(candidateId:string)=>offers.find(x=>x.candidateId===candidateId),projectionFor=(candidateId:string)=>offerProjections.find(x=>x.candidateId===candidateId);return <section className="lg:col-span-2 rounded-xl border-2 border-[#0F2B46] bg-white p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="text-lg font-black text-[#0F2B46]">D3 Smart Matches + D4 Manual Dispatch</h3><p className="text-sm text-slate-600">Manual WhatsApp availability checks only. No automated send or assignment.</p></div><a href={`/admin/dispatch?open=${bookingDocumentId}&matches=${bookingDocumentId}`} className="rounded-lg bg-[#0F2B46] px-4 py-2 text-sm font-bold text-white">Refresh matches</a></div><p className="mt-2 text-xs text-slate-500">Conflict window: {projection.window.start} → {projection.window.end}. {projection.window.basis}.</p>{offers.length>0&&<div className="mt-4 rounded-xl bg-slate-100 p-3"><h4 className="font-black">Driver Responses</h4><div className="mt-2 grid gap-2 text-sm">{offers.map(offer=><div key={offer.id} className="rounded bg-white p-2"><b>{offer.driverName}</b> — {offer.vendorName} — {offer.vehicleRegistration} · <b>{offer.responseStatus==="available"?"🟢 AVAILABLE":offer.responseStatus==="declined"?"🔴 DECLINED":offer.responseStatus==="no_response"?"⚪ NO RESPONSE":offer.offerStage.replaceAll("_"," ")}</b>{offer.responseAt&&` · ${new Date(offer.responseAt).toLocaleString("en-PK")}`}</div>)}</div><p className="mt-2 text-xs font-bold">Availability only — no driver or vehicle is assigned.</p></div>}{!projection.ready?<p className="mt-4 rounded-lg bg-amber-50 p-3 font-bold text-amber-900">Matching unavailable: {projection.unavailableReason}. Historical responses remain visible; new offer actions are disabled.</p>:<><h4 className="mt-5 font-black">Recommended Top {projection.top.length}</h4><div className="mt-2 grid gap-3">{projection.top.map((candidate,index)=><CandidateCard key={candidate.id} bookingDocumentId={bookingDocumentId} candidate={candidate} rank={index+1} offerProjection={projectionFor(candidate.id)} offer={offerFor(candidate.id)}/>)}</div>{!projection.top.length&&<p className="mt-2 rounded-lg bg-amber-50 p-3">No eligible combination was found. Review diagnostics below.</p>}<details className="mt-4 rounded-lg border p-3"><summary className="cursor-pointer font-bold">View all eligible ({projection.eligible.length})</summary><div className="mt-3 grid gap-2">{projection.eligible.map(candidate=><div key={candidate.id}><CandidateCard bookingDocumentId={bookingDocumentId} candidate={candidate} offerProjection={projectionFor(candidate.id)} offer={offerFor(candidate.id)}/>{!candidate.manuallyIncluded&&<form action={setMatchOverrideAction} className="mt-1 text-right"><input type="hidden" name="bookingDocumentId" value={bookingDocumentId}/><input type="hidden" name="candidateId" value={candidate.id}/><input type="hidden" name="mode" value="include"/><button className="rounded-lg border bg-white px-3 py-1.5 text-xs font-bold">Prioritize</button></form>}</div>)}</div></details><details className="mt-3 rounded-lg border p-3"><summary className="cursor-pointer font-bold">Excluded / Not Eligible ({projection.excluded.length})</summary><div className="mt-3 space-y-2">{projection.excluded.map((item,index)=><div key={`${item.kind}:${item.id}:${index}`} className="flex flex-wrap items-center justify-between gap-2 rounded-lg bg-slate-50 p-2 text-sm"><span><b>{item.kind}:</b> {item.label} — {item.reason}{item.critical&&" (critical block)"}</span>{item.kind==="candidate"&&!item.critical&&<form action={setMatchOverrideAction}><input type="hidden" name="bookingDocumentId" value={bookingDocumentId}/><input type="hidden" name="candidateId" value={item.id}/><input type="hidden" name="mode" value="include"/><button className="rounded border bg-white px-2 py-1 text-xs font-bold">Include again</button></form>}</div>)}</div></details></>}</section>}
