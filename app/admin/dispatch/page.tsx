import type { Metadata } from "next";import Link from "next/link";import{redirect}from"next/navigation";import{hasAdminSession}from"../_lib/session";import DispatchAdminNav from"../_components/DispatchAdminNav";import{input,label}from"../_components/DispatchFormFields";import{listOperationalBookings,listOperationalEvents}from"@/lib/dispatch/booking-repository";import{OPERATIONAL_SOURCE_TYPES}from"@/lib/dispatch/booking-types";import{NORMAL_RENTAL_ZONES}from"@/lib/normal-rental/zones";import{applyCustomerDiscountAction,cancelBookingAction,createManualBookingAction,importBookingAction,overridePaymentAction,recordPaymentAction,reviewPayoutAction}from"./actions";import{formatDispatchAdminDate}from"@/lib/dispatch/validation";import ManualBookingForm from"./ManualBookingForm";import{listDispatchVehicles}from"@/lib/dispatch/vehicles";
export const metadata:Metadata={title:"Dispatch Queue | RentKA Admin",robots:{index:false,follow:false,noarchive:true,nosnippet:true}};const money=(n:number)=>`Rs ${(n/100).toLocaleString("en-PK",{minimumFractionDigits:0,maximumFractionDigits:2})}`;const tabs=["all","awaiting_advance","advance_recorded","needs_vendor_payout","ready_for_dispatch","override_approved","cancelled"] as const;const badge=(s:string)=>s==="ready_for_dispatch"?"bg-green-100 text-green-800":s==="cancelled"?"bg-red-100 text-red-800":s==="override_approved"?"bg-purple-100 text-purple-800":"bg-amber-50 text-amber-900";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){if(!(await hasAdminSession()))redirect("/admin/pricing-calculator");const q=await searchParams;const[all,vehicles]=await Promise.all([listOperationalBookings(),listDispatchVehicles()]);const rows=all.filter(b=>(!q.tab||q.tab==="all"||(q.tab==="advance_recorded"?b.customerFinancials.paymentStatus==="sufficient":b.readinessStatus===q.tab))&&(!q.source||b.source.type===q.source)&&(!q.zone||b.zoneId===q.zone)&&(!q.date||b.itinerary.travelDate===q.date)&&(!q.search||`${b.bookingId} ${b.customer.name} ${b.customer.phone}`.toLowerCase().includes(q.search.toLowerCase())));const events=q.open?await listOperationalEvents(q.open):[];return <main className="min-h-screen bg-slate-100 p-4 md:p-8"><div className="mx-auto max-w-7xl"><DispatchAdminNav current="dispatch"/><header className="mb-6"><p className="text-sm font-bold uppercase tracking-widest text-[#5BAE4A]">RentKA operations</p><h1 className="text-3xl font-black text-[#0F2B46]">Dispatch intake queue</h1><p className="text-slate-600">Payment and payout readiness only. D2 does not contact or assign drivers.</p></header>{q.error&&<p role="alert" className="mb-4 rounded-lg bg-red-50 p-3 text-red-800">{q.error}</p>}{q.message&&<p className="mb-4 rounded-lg bg-green-50 p-3 text-green-800">{q.message}</p>}
<div className="mb-6 grid gap-5 lg:grid-cols-2"><details className="rounded-2xl bg-white p-5 shadow-sm"><summary className="cursor-pointer text-xl font-black text-[#0F2B46]">+ Create Manual Booking</summary><ManualBookingForm action={createManualBookingAction} vehicles={vehicles}/></details>
<details className="rounded-2xl bg-white p-5 shadow-sm"><summary className="cursor-pointer text-xl font-black text-[#0F2B46]">Import Existing Booking</summary><p className="mt-2 text-sm text-slate-600">Enter the Firestore source document ID. Re-importing the same source is safe and will not duplicate it.</p><form action={importBookingAction} className="mt-4 grid gap-3"><label className={label}>Source<select className={input} name="sourceType">{OPERATIONAL_SOURCE_TYPES.filter(x=>x!=="manual").map(x=><option key={x} value={x}>{x.replaceAll("_"," ")}</option>)}</select></label><label className={label}>Source document ID<input className={input} required name="sourceDocumentId"/></label><button className="rounded-lg bg-[#0F2B46] px-5 py-3 font-bold text-white">Import into queue</button></form></details></div>
<nav className="mb-3 flex flex-wrap gap-2">{tabs.map(t=><Link key={t} href={`/admin/dispatch?tab=${t}`} className={`rounded-full px-3 py-2 text-xs font-bold ${q.tab===t||(!q.tab&&t==="all")?"bg-[#0F2B46] text-white":"bg-white"}`}>{t.replaceAll("_"," ")}</Link>)}</nav><form className="mb-5 grid gap-2 rounded-xl bg-white p-3 sm:grid-cols-5"><input className={input} name="search" placeholder="ID, customer or phone" defaultValue={q.search}/><select className={input} name="source" defaultValue={q.source??""}><option value="">All sources</option>{OPERATIONAL_SOURCE_TYPES.map(x=><option key={x}>{x}</option>)}</select><select className={input} name="zone" defaultValue={q.zone??""}><option value="">All zones</option>{Object.entries(NORMAL_RENTAL_ZONES).map(([id,z])=><option key={id} value={id}>{z.label}</option>)}</select><input className={input} name="date" placeholder="YYYY-MM-DD" defaultValue={q.date}/><button className="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form>
<section className="grid gap-4">{rows.map(b=><article key={b.id} className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex flex-wrap justify-between gap-3"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-black text-[#0F2B46]">{b.bookingId}</h2><span className={`rounded-full px-2 py-1 text-xs font-bold ${badge(b.readinessStatus)}`}>{b.readinessStatus.replaceAll("_"," ")}</span>{b.paymentOverride?.approved&&<span className="rounded-full bg-purple-100 px-2 py-1 text-xs font-bold text-purple-800">Payment override</span>}</div><p className="mt-1 text-sm">{b.source.type.replaceAll("_"," ")} · {NORMAL_RENTAL_ZONES[b.zoneId].label} · {b.serviceType}</p></div><Link href={`/admin/dispatch?open=${b.id}`} className="h-fit rounded-lg border px-4 py-2 text-sm font-bold">Open / Review</Link></div><div className="mt-4 grid gap-3 text-sm md:grid-cols-3"><div><p className="text-slate-500">Customer</p><p className="font-bold">{b.customer.name}</p><p>{b.customer.phone}</p></div><div><p className="text-slate-500">Trip</p><p className="font-bold">{formatDispatchAdminDate(b.itinerary.travelDate)} · {b.itinerary.pickupTime}</p><p>{b.itinerary.pickup} → {b.itinerary.destinationOrUsage||"Usage to confirm"}</p></div><div><p className="text-slate-500">Requested vehicle</p><p className="font-bold">{b.requestedVehicle.categoryOrModel}</p>{b.requestedVehicle.passengers&&<p>{b.requestedVehicle.passengers} passengers</p>}</div></div><div className="mt-4 grid gap-3 rounded-xl bg-blue-50 p-4 text-sm sm:grid-cols-4 lg:grid-cols-7"><div><p className="text-slate-500">Original sale</p><b>{money(b.customerFinancials.originalCustomerTotalMinor??b.customerFinancials.customerTotalMinor)}</b></div><div><p className="text-slate-500">Discounts</p><b>{money(b.customerFinancials.customerDiscountTotalMinor??0)}</b></div><div><p className="text-slate-500">Final sale</p><b>{money(b.customerFinancials.finalCustomerTotalMinor??b.customerFinancials.customerTotalMinor)}</b></div><div><p className="text-slate-500">Required advance</p><b>{money(b.customerFinancials.requiredAdvanceMinor)}</b></div><div><p className="text-slate-500">Received</p><b>{money(b.customerFinancials.receivedAmountMinor)}</b></div><div><p className="text-slate-500">Balance</p><b>{money(b.customerFinancials.balanceMinor)}</b></div><div><p className="text-slate-500">Refund / credit due</p><b className={(b.customerFinancials.refundCreditDueMinor??0)>0?"text-red-700":""}>{money(b.customerFinancials.refundCreditDueMinor??0)}</b></div></div><div className="mt-3 rounded-xl bg-slate-50 p-3 text-sm"><b>Internal vendor payout:</b> {b.internalFinancials.payoutStatus==="reviewed"?money(b.internalFinancials.vendorPayoutMinor??0):"Needs review"}</div>
{q.open===b.id&&<div className="mt-5 grid gap-4 border-t pt-5 lg:grid-cols-2"><form action={recordPaymentAction} className="rounded-xl border p-4"><input type="hidden" name="bookingDocumentId" value={b.id}/><h3 className="font-black">Record Payment</h3><div className="mt-3 grid gap-2 sm:grid-cols-2"><label className={label}>Amount (Rs)<input className={input} required name="amount" inputMode="decimal"/></label><label className={label}>Method<select className={input} name="method"><option value="">Not specified</option><option>Bank transfer</option><option>Cash</option><option>JazzCash</option><option>Easypaisa</option><option>Other</option></select></label><label className={label}>Reference<input className={input} name="reference"/></label><label className={label}>Note<input className={input} name="note"/></label></div><button className="mt-3 rounded-lg bg-blue-700 px-4 py-2 font-bold text-white">Record payment</button></form><form action={applyCustomerDiscountAction} className="rounded-xl border border-amber-200 bg-amber-50 p-4"><input type="hidden" name="bookingDocumentId" value={b.id}/><h3 className="font-black">Apply Customer Discount / Adjustment</h3><p className="mt-1 text-xs text-slate-600">Reduces final customer sale only. Vendor payout is unchanged.</p><label className={`${label} mt-3 block`}>Discount amount (Rs)<input className={input} required name="discountAmount" inputMode="decimal"/></label><label className={`${label} mt-2 block`}>Mandatory reason<input className={input} required minLength={3} name="discountReason" placeholder="Goodwill, service recovery, pricing correction…"/></label><button className="mt-3 rounded-lg bg-amber-700 px-4 py-2 font-bold text-white">Apply discount</button></form><form action={reviewPayoutAction} className="rounded-xl border p-4"><input type="hidden" name="bookingDocumentId" value={b.id}/><h3 className="font-black">Review Vendor Payout</h3><label className={`${label} mt-3 block`}>Approved payout (Rs)<input className={input} required name="vendorPayout" inputMode="decimal" defaultValue={b.internalFinancials.vendorPayoutMinor!==undefined?b.internalFinancials.vendorPayoutMinor/100:""}/></label><label className={`${label} mt-2 block`}>Internal payout notes<input className={input} name="payoutNotes" defaultValue={b.internalFinancials.payoutNotes}/></label><button className="mt-3 rounded-lg bg-green-700 px-4 py-2 font-bold text-white">Approve payout review</button></form><form action={overridePaymentAction} className="rounded-xl border border-purple-200 bg-purple-50 p-4"><input type="hidden" name="bookingDocumentId" value={b.id}/><h3 className="font-black text-purple-900">Dispatch Before Payment</h3><p className="mt-1 text-xs text-purple-800">Does not bypass vendor payout review.</p><label className={`${label} mt-3 block`}>Mandatory exception reason<input className={input} required minLength={8} name="overrideReason"/></label><button className="mt-3 rounded-lg bg-purple-800 px-4 py-2 font-bold text-white">Approve override</button></form><form action={cancelBookingAction} className="rounded-xl border border-red-200 p-4"><input type="hidden" name="bookingDocumentId" value={b.id}/><h3 className="font-black text-red-800">Stop Processing</h3><select className={input} name="cancellationType"><option value="not_proceeding">Not proceeding</option><option value="cancelled">Cancelled</option></select><label className={`${label} mt-2 block`}>Reason<input className={input} required name="cancellationReason"/></label><button className="mt-3 rounded-lg bg-red-700 px-4 py-2 font-bold text-white">Confirm stop</button></form><details className="lg:col-span-2 rounded-xl border p-4"><summary className="cursor-pointer font-black">Audit history ({events.length})</summary><div className="mt-3 space-y-2">{events.map(e=><div key={String(e.id)} className="rounded-lg bg-slate-50 p-2 text-sm"><b>{String(e.type).replaceAll("_"," ")}</b> · {String(e.timestamp)}<pre className="mt-1 overflow-auto text-xs">{JSON.stringify(e.metadata??{},null,2)}</pre></div>)}</div></details></div>}</article>)}{!rows.length&&<p className="rounded-xl border border-dashed bg-white p-8 text-center text-slate-500">No operational bookings match these filters.</p>}</section></div></main>}
