"use server";
import { revalidatePath } from "next/cache"; import { redirect } from "next/navigation";
import { hasAdminSession } from "../_lib/session"; import { parseDriverForm } from "@/lib/dispatch/validation"; import { saveDispatchDriver } from "@/lib/dispatch/drivers";
export async function saveDriver(form: FormData) { if (!(await hasAdminSession())) throw new Error("Unauthorized"); const id=String(form.get("id")??"").trim()||undefined,vendorContext=String(form.get("vendorContext")??"").trim(),scope=vendorContext?`vendor=${encodeURIComponent(vendorContext)}&`:""; try { await saveDispatchDriver(id,parseDriverForm(form)); } catch(error){redirect(`/admin/drivers?${scope}${id?`edit=${encodeURIComponent(id)}&`:""}error=${encodeURIComponent(error instanceof Error?error.message:"Unable to save driver.")}`)} revalidatePath("/admin/drivers"); revalidatePath("/admin/vendors"); redirect(`/admin/drivers?${scope}saved=Driver saved`); }
