import { NextResponse } from "next/server";import { hasAdminSession } from "@/../app/admin/_lib/session";import { getPartnerApplication } from "@/lib/partner-applications/repository";import { readProtectedDocument } from "@/lib/partner-applications/documents";import { protectedDocumentReadFailure,resolvePartnerApplicationDocument } from "@/lib/partner-applications/document-retrieval-core";
export const runtime="nodejs";export async function GET(request:Request){if(!(await hasAdminSession()))return NextResponse.json({error:"Unauthorized"},{status:401});const url=new URL(request.url),id=url.searchParams.get("application"),documentId=url.searchParams.get("document");if(!id||!documentId)return NextResponse.json({error:"Missing document reference"},{status:400});const application=await getPartnerApplication(id),reference=resolvePartnerApplicationDocument(application,documentId);if(!reference)return NextResponse.json({error:"Document not found"},{status:404});try{const file=await readProtectedDocument(reference.document.storagePath);return new NextResponse(new Uint8Array(file.buffer),{headers:{"Content-Type":file.contentType,"Content-Disposition":`inline; filename="${file.name.replace(/\"/g,"")}"`,"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}})}catch(error){const failure=protectedDocumentReadFailure(error);return NextResponse.json({error:failure.message},{status:failure.status})}}
