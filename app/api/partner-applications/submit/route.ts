import { NextResponse } from "next/server";import { validateSubmission,PartnerValidationError } from "@/lib/partner-applications/validation";import { createPartnerApplication } from "@/lib/partner-applications/repository";import { cleanupExpiredStagedUploads } from "@/lib/partner-applications/documents";
export const runtime="nodejs";export async function POST(request:Request){try{void cleanupExpiredStagedUploads(10).catch(()=>{});const body=await request.json() as Record<string,unknown>,input=validateSubmission(body),result=await createPartnerApplication(input);return NextResponse.json({success:true,...result})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Application could not be submitted.",field:error instanceof PartnerValidationError?error.field:undefined},{status:error instanceof PartnerValidationError?400:500})}}
