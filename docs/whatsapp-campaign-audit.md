# C1 read-only audit — 2026-09-09

Completed before implementation. No production records were read or mutated; this is a source/schema and environment-name audit, not a live Firestore rules audit.

- Admin routes: pricing, pricing-calculator, airport-pricing, ta-connections, vendors, vehicles, drivers, dispatch and partner-applications. Pages and server actions check `hasAdminSession`; signed HMAC cookie expires after 10 hours, using RENTKA_ADMIN_PASSWORD. Shared admin identity, not individual users. No common admin layout exists; pages share styling/navigation.
- Firebase Admin uses FIREBASE_PROJECT_ID, FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY and optional FIREBASE_STORAGE_BUCKET. These variable names are present locally; values were not printed. Browser Firebase modules also exist. Production Firestore security rules are not in this checkout and must deny browser access to campaign collections before release.
- Customer data is embedded in leads, airportBookings, manualBookings/operationalBookings and taConnectionsBookings. No reusable marketing contact model found. Supply collections include dispatchVendors, dispatchDrivers, dispatchVehicles, normalRentalVendors and countries/{country}/vendors. No source records will be imported automatically.
- Messaging: whatsappOutboundMessages is operational outbox storage; metaWhatsAppMessages holds delivery status; metaWhatsAppWebhookEvents and dualhookWhatsAppWebhookEvents retain webhook events. Driver broadcasts/offers are under operationalBookings. Existing delivery reconciliation only handles driver_offer jobs.
- Meta webhook: /api/webhooks/meta/whatsapp, challenge verification and HMAC signature verification. Dualhook: /api/webhooks/dualhook/whatsapp/[secret], secret path plus GET verification token and configured account filtering. Preserve both handlers and operational reconciliation.
- Present environment names: DUALHOOK_WHATSAPP_API_KEY, DUALHOOK_WHATSAPP_WEBHOOK_VERIFY_TOKEN, DUALHOOK_WHATSAPP_WEBHOOK_PATH_SECRET, META_WHATSAPP_WEBHOOK_VERIFY_TOKEN, META_WHATSAPP_APP_SECRET, META_WHATSAPP_ACCESS_TOKEN, META_WHATSAPP_WABA_ID, META_WHATSAPP_PHONE_NUMBER_ID, META_WHATSAPP_DRIVER_OFFER_TEMPLATE_NAME, META_WHATSAPP_DRIVER_OFFER_TEMPLATE_LANGUAGE, RENTKA_PUBLIC_BASE_URL. Existing outbound adapter uses https://api.dualhook.com with connection-scoped bearer configuration. No provider call made.
- Airport phone helper strips display punctuation and checks shape. Dispatch helper produces digits without plus and accepts ambiguous international input. Leave both unchanged; campaign normalization needs stricter international metadata validation.
- No CSV import utility or real Google Contacts CSV export supplied. Implement Name/Phone first; document explicitly supported Google header variants without claiming validation against the user's export.
- Audit convention: events subcollections, server timestamps, actor {type: shared_admin_session}, type and metadata. Reuse this shape for campaign/contact mutations.
- Existing uncommitted dispatch/supply edits predate this task and are outside its scope.
- Only pasted-text.txt was attached. No approved campaign header asset was supplied or found by its campaign name in public. Do not substitute or recreate it.
